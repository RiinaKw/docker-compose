version: "3"

services:
    web:
        # locaolhost:12000 で PHP が動く web サーバ
        image: php:8.1-apache
        container_name: example_web
        volumes:
            - ./app:/var/www/html
        ports:
            - 12000:80
